# 明白金 FinFair 最终项目报告

> 版本：草稿 V0.1
> 状态：随项目逐项优化持续更新
> 注意：本文件只记录已有证据支持的成果；空缺项保留“待完成”，不提前包装成结果。

## 1. 项目定位和当前版本

明白金是一款金融产品购买前信息核验 Agent：它将宣传材料与正式说明书逐项对照，把收益、本金、期限、费用和风险翻译成普通用户能理解的说明，并为每个结论提供可追溯证据。

当前版本为可运行的课程 MVP，首要用户是普通金融消费者。金融机构营销材料初审是未来 B2B 延伸设想，不属于当前已验证交付。

## 2. 技术与产品选择

- 前端与应用框架：Streamlit；
- PDF 文字解析：`pdfplumber`，保留页面边界；
- 确定性分析：Python 规则引擎；
- 语义增强：可选的分析 Agent 与核验 Agent；
- 防幻觉：独立核验加程序逐字引用门控；
- 模型接入：用户自行提供 API 配置；
- 导出：Markdown、JSON、Word 和 PDF。

选择混合模式的原因是：无 API 时仍可稳定演示，复杂语义可以由模型补充，但模型新增内容不能绕过证据核验。

## 3. AI 参与和团队人工判断

### AI 参与

- 辅助整理需求、文档结构和测试思路；
- 在用户主动配置模型后分析复杂金融语义；
- 核验 Agent 独立审查分析 Agent 的候选内容。

### 必须由团队承担的判断

- 决定项目范围、风险边界和课堂表达；
- 设计模拟材料与人工标准答案；
- 审查规则、提示词和模型输出；
- 确认引用、页面效果、演示脚本和最终提交内容；
- 不把模型输出当作法律、合规或投资结论。

团队成员与具体分工：**待补充**。

## 4. Git 开发过程

当前仓库已形成至少两个主要版本节点：

- `099cc1a`：混合 Agent MVP；
- `bf69f73`：扩展模型支持并完善课程 README。

本轮文档规范化尚未提交。最终报告应在提交前补充实际 Commit、贡献者和版本链接，不虚构团队贡献。

## 5. 自动化与人工验证

当前已有规则分析、缺字段、宣传冲突、端点安全、Anthropic 协议、双 Agent 和逐字引用门控测试。

2026-07-25 本轮结果：

- 规则与 Agent 相关 pytest：31 项通过、1 项跳过；
- Streamlit AppTest：2 项通过；
- 模拟标准答案基准集：12/12 案例通过；
- 完整依赖环境的 PDF 主流程：通过；
- 桌面端与 375px 手机端页面：通过；
- Markdown、JSON、Word、PDF 四格式本地生成：通过；
- 线上匿名访问、内置案例、Markdown 与 JSON 下载：通过。

## 6. 部署地址

- 公开地址：<https://finfair-agent.streamlit.app/>
- 平台：Streamlit Community Cloud 免费方案；
- 仓库与分支：`XiAn-07/FinFair`，`main`；
- 部署提交：`de41875`；
- Python：3.13；
- 部署与复测日期：2026-07-25；
- 证据：`screenshots/deployed-site.png`、`screenshots/deployed-mobile.png`。

匿名访问已验证；线上规则模式成功解析内置 4 页模拟 PDF，提取 8 个核心字段并识别 6 项宣传核验问题。Markdown 与 JSON 下载均触发成功。线上部署仍遵循 BYOK：平台不保存团队共享模型密钥，未配置 API 时使用规则模式。

## 7. 问题、修复和复测

| 问题 | 处理 | 影响文件 | 状态 |
| --- | --- | --- | --- |
| 产品同时混讲消费者和机构用户 | 当前聚焦消费者，B2B 标为后续方向 | `README.md`、`app.py`、`项目制作方案.md` | 已修复并完成文本复测 |
| 商业设想容易被误写成已验证结论 | 付费、转化和采购统一列为项目限制 | `README.md`、`项目制作方案.md` | 已修复并完成文本复测 |
| 宣传核验藏在次级页签且状态不统一 | 升级为首要结果，增加五种稳定状态并同步四种报告 | `finfair/core.py`、`app.py`、`finfair/report_export.py` | 自动化场景通过；待浏览器截图 |
| 用户无法判断系统是否分析全文 | 增加规则解析、空白页、Agent 字符数及截断披露 | `finfair/core.py`、`finfair/llm_agent.py`、`app.py`、`finfair/report_export.py` | 长文、空页、全空文档和 API 失败场景通过 |
| 双阶段 Agent 过程不够直观 | 展示四阶段状态、模型协议、候选/核验/门控/拦截计数和停止原因 | `finfair/core.py`、`finfair/llm_agent.py`、`app.py` | 固定两次调用、空候选停止和虚构引用拦截测试通过 |
| 只有代码用例数，缺少可解释质量指标 | 建立12例模拟标准答案集和带分母的规则模式指标 | `tests/fixtures/benchmark_cases.json`、`scripts/run_benchmark.py`、`report/benchmark-results.json` | 12/12通过；结果仅适用于该模拟集 |
| 页面层级与手机端体验不足 | 重构首屏、双入口、三步输入、核心结果和下载区，增加安全动态文本渲染 | `app.py`、`.streamlit/config.toml`、`tests/test_ui.py` | 1440px与375px无页面横向溢出，四个下载入口可见 |
| 当前终端缺少 `pdfplumber` | 保留失败记录，并用完整依赖环境单独复测 PDF 主流程 | 无代码改动 | 完整依赖环境复测通过 |

后续问题按 `docs/checklist.md` 逐项记录，取得证据后才能关闭。

## 8. 已知限制

- 仅支持有可复制文字的 PDF，不支持纯扫描件 OCR；
- 单次分析一个产品；
- 规则覆盖有限，模型输出也可能不稳定；
- 模型文档输入上限为 45,000 字符；发生截断时页面和报告会提示，截断后的 Agent 结果不能视为全文审查；
- 引用存在不等于解释必然正确；
- 主要测试材料为模拟案例，不能代表全部真实金融产品；
- 不处理真实客户敏感资料；
- 不构成投资建议、收益承诺、法律意见、正式合规审查或适当性评估。

## 9. 市场对比

市场上已有通用大模型、PDF 问答、合同风险识别、金融文件分析和费用分析工具。本项目不声称“没有竞品”。当前差异化设想是把宣传材料—正式说明书对照、通俗解释和页码证据组合到中国金融消费者购买前场景。

竞品事实、功能与最新状态应在制作 PPT 前重新核验并保留来源；未核验内容不能写成确定结论。

## 10. 后续计划

按本地优化方案顺序推进：

1. 将宣传材料对照升级为核心结果；
2. 增加覆盖率和截断提示；
3. 加强双 Agent 结构化校验；
4. 扩充模拟样本与评测；
5. 完成桌面端、手机端和导出复测；
6. 使用免费平台部署；
7. 制作不超过 5 分钟的汇报和不超过 2 分钟的 Demo 视频。

以上是计划，不是已完成成果。

## 11. 隐私与风险边界

- 只使用公开模拟或已获授权、已脱敏材料；
- API Key 不进入仓库或报告；
- 启用第三方模型前明确告知数据将发送给相应服务商；
- 模型结果必须有证据并接受人工复核；
- 系统不替用户或金融机构作投资、适当性、法律或合规决定；
- 正式材料、监管要求和专业人员判断优先于本工具输出。
