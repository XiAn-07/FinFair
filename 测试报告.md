# FinFair MVP 测试报告

> 测试日期：2026年7月25日  
> 测试范围：银行理财产品 PDF 解析、核心字段提取、宣传材料一致性检查、混合 Agent、证据追溯和 Markdown 导出  
> 结论：核心演示闭环可用，可进入 PPT、商业计划书和 Demo 视频制作阶段。

## 1. 用户实际测试

- 输入文件：`sample_data/模拟理财产品说明书.pdf`
- 宣传材料：内置模拟宣传文案
- 导出文件：`金融产品公平说明书 test1.md`
- 解析结果：4 页 PDF、8 个核心字段、6 项宣传材料问题
- 导出结果：Markdown 文件结构完整、中文编码正常、免责声明完整

## 2. 发现并修复的问题

### PDF 换行导致证据显示为半句话

首次导出中出现了以下截断：

- “资产信用状”
- “提前取回投资本”

原因是 PDF 将一个自然段拆成了多行，旧版本只返回命中的单行。

现已增加跨行证据合并：在不跨页、不跨表格和不跨新标题的前提下，将同一条款的后续行拼接为完整证据。修复后分别显示为：

- “资产信用状况变化、流动性不足以及其他风险因素发生损失。”
- “投资者不能因临时资金需要而提前取回投资本金。”

## 3. 三类核心测试

| 场景 | 输入特征 | 预期行为 | 当前结果 |
|---|---|---|---|
| 标准案例 | 字段和风险披露完整，宣传材料故意弱化风险 | 提取 8 个字段，发现 6 项宣传问题，并附页码证据 | 通过 |
| 信息遗漏 | 说明书仅包含产品名称，缺少收益、期限和退出等信息 | 显示“当前材料未找到/待人工确认”，不得自行补全 | 通过 |
| 宣传冲突 | 宣传写“保本稳赚、年化 3.20%”，说明书明确不保本 | 标记确定性收益表达、未披露不保本等问题 | 通过 |

## 4. 混合 Agent 测试

当前版本已加入用户自行配置的 OpenAI 兼容接口，并通过模拟模型响应完成以下测试：

| 测试项 | 验收标准 | 当前结果 |
|---|---|---|
| 双逻辑 Agent | 语义分析 Agent 和证据核验 Agent 各调用一次 | 通过 |
| JSON 结构检查 | 非 JSON 或结构错误时不进入正式结果 | 通过 |
| 逐字引用校验 | 模型引用必须真实存在于所标页码 | 通过 |
| 幻觉拦截 | 即使核验 Agent 误判，虚构引文仍被程序拦截 | 通过 |
| 接口安全 | 拒绝非 HTTPS、本机和私有 IP 地址 | 通过 |
| 无 API 降级 | 未配置或调用失败时保留规则结果 | 通过 |
| 页面流程 | 规则模式 6 个结果页签和 4 个指标正常展示 | 通过 |

自动化测试结果：`5 passed`。

## 5. 当前质量边界

- 仅支持含可复制文字的 PDF，扫描件尚未接入 OCR；
- 规则模式能稳定处理演示案例，混合 Agent 用于补充复杂语义；
- 大模型输出存在随机性，只有通过模型核验和程序化逐字引用校验的洞察才会展示；
- 页码证据用于辅助核验，正式使用仍需人工复核；
- 系统不判断“是否值得购买”，不构成投资建议或正式适当性评估。

## 6. 下一阶段

产品端已经具备课堂演示所需的最小闭环。下一阶段应集中制作：

1. 8—10 页商业计划书/PPT；
2. 3 分钟 Demo 视频脚本；
3. GitHub 仓库说明和团队分工；
4. 产品、PPT、README、视频之间的一致性核验。
